
<%#= form.fields_for :feast_name_attributes do |fnf| %>
<%= form.fields_for :feast_names do |fnf| %>

  <div class="field">
    <%= fnf.label :name_id, 'Name' %>
    <%= fnf.collection_select :name_id, Name.all, :id, :name, include_blank: true %>
    <%#= fnf.select :name_id, Name.all.collect {|p| [ p.name, p.id ] } %>
  </div>

  <div class="field">
    <%= fnf.label :saint_attributes %>
    <%= fnf.collection_check_boxes :saint_attributes, FeastName::SAINT_ATTRIBUTES, :to_s, :to_s %>
    <!-- , :first, :last -->
  </div>

  <div class="field">
    <%= fnf.label :saint_location %>
    <%= fnf.text_field :saint_location %>
  </div>

<% end %>

<div class="field">
  <%= form.label :modifier %>
  <%= form.select :modifier, Feast::MODIFIERS, include_blank: true %>
</div>


<div class="field">
  <%= form.label :shading %>
  <%= form.text_field :shading %>
</div>


<div class="field colors">
  <%= form.label :color%>
  <%= form.select :color, colors_for_manuscript(@calendar_page.manuscript) %>
</div>

<!--
<select>
  <% Manuscript::COLORS.each do |color| %>
    <option style=<%="color:#{color.downcase};"%> value=<%= color %> id=<%= 'color_' + color.downcase %><%= color %></option>
  <%  end %>
</select> -->

<div class="field">
  <%= form.label :transcription %>
  <%= form.text_area :transcription %>
</div>

<br>

<%= form.hidden_field :month_number %>
<%= form.hidden_field :day_number %>
<%= form.hidden_field :manuscript_id %>

<div class="field">
  <%= form.submit %>
</div>

