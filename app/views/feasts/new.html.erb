<div class="container" id="feast-form-area">
  <% options = {} #{html: {class: "form bordered-modal"}} %>
  <% options.merge!({remote: true, format: :js}) if request.xhr? %>

  <%= form_for [@calendar_page, @feast], options do |form| %>
    <%= render 'form', form: form %>
  <% end %>



  <%#= link_to 'back', [@manuscript, @calendar_page], class: "btn btn-kal-standard" unless request.xhr? %>
</div>


<!-- name modal -->
<div class="modal fade" id="name-modal" tabindex="-1" role="dialog">
  <div class="modal-dialog modal-lg bordered-modal" role="document">
    <div class="modal-content">
      <div class="modal-header" style="font-size: 30px; background-color: #fec">
        <h4>New Name</h4>
      </div>
      <div class="modal-body container">
        [...]
      </div>
      <div class="btn btn-kal-standard" id="hide-modal"> cancel </div>
    </div>
  </div>
</div>