<h1><%= "#{Kal::Months::MONTH_TABLE.find{ |m| m.number == @feast.month_number }.name} #{@feast.day_number}" %></h1>

<%= form_for @feast do |form| %>
  <%= render 'form', form: form %>
<% end %>

<%= link_to 'Back', calendar_page_path(@feast.calendar_page) %>
