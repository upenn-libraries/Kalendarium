<h1><%= "#{month_name(@feast.month_number)} #{@feast.day_number}" %></h1>

<%= form_for @feast do |form| %>
  <%= render 'form', form: form %>
<% end %>

<%= link_to 'Back', calendar_page_path(@feast.calendar_page) %>
